{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "title": "Loans overdue status request",
  "description": "Loans overdue status request",
  "properties": {
    "loanIds": {
      "description": "Array of loan ids",
      "id": "loanIds",
      "type": "array",
      "items": {
        "title": "Loan id",
        "description": "UUID of a loan",
        "type": "string",
        "$ref": "uuid.json"
      }
    }
  },
  "required": [
    "loanIds"
  ]
}
